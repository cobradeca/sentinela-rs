# Arquivos de design — Sentinela RS

Copie estes arquivos para o projeto mantendo os mesmos caminhos.

Depois rode:

```cmd
npm run build
git add src/main.jsx src/styles/sentinela-ui.css
git commit -m "Aplica design operacional ao Sentinela RS"
git push origin main
```
